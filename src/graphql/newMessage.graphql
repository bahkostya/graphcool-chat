subscription Message {
    Message(filter: { mutation_in: [CREATED] }) {
        node {
            id
            createdAt
            text
            author {
                id
                firstName
                photo
            }
        }
    }
}
